-- Crear la Base de Datos
CREATE DATABASE ejercicio7
charset utf8mb4
collate utf8mb4_spanish2_ci;

-- Activar la Base de Datos
USE ejercicio7;

-- Crear la tabla departamentos

CREATE TABLE departamentos (
	idDepartamento INT AUTO_INCREMENT,
    nombreDepartamento VARCHAR(45),
    PRIMARY KEY (idDepartamento)
);

-- Crear la tabla empleados

CREATE TABLE empleados (
	idEmpleado INT AUTO_INCREMENT,
    nombreEmpleado VARCHAR (45),
    primerApellidoEmpleado VARCHAR (45),
    segundoApellidoEmpleado VARCHAR (45),
    cargoEmpleado VARCHAR(45),
    dniEmpleado VARCHAR(9),
    idDepartamentoFK INT,
    PRIMARY KEY (idEmpleado),
    FOREIGN KEY (idDepartamentoFK)
        REFERENCES departamentos (idDepartamento)
);

-- Insertar datos en la tabla.
-- Insertar mediante los 3 tipos de registros:
INSERT INTO departamentos VALUES(
	null, 'Administración'
);

-- COPIAR Y PEGARLE LA FORMA DE INSERTAR DATOS DE MANERA MANUAL - EJEMPLO:
/*
INSERT INTO `ejercicio7`.`departamentos` (`nombreDepartamento`) VALUES ('Comercial');
INSERT INTO `ejercicio7`.`departamentos` (`nombreDepartamento`) VALUES ('Limpieza');
INSERT INTO `ejercicio7`.`departamentos` (`nombreDepartamento`) VALUES ('Logística');
*/


-- Insertar registros manualmente y aplicando los cambios
-- Insertar registros mediante el csv
SELECT * FROM departamentos;
SELECT * FROM empleados;

-- Error al meter datos. Borrar registro pero no tabla.
TRUNCATE empleados;

SELECT * FROM empleados;


