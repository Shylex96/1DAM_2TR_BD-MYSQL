-- Crear la Base de Datos
CREATE DATABASE ejercicio5
charset utf8mb4
collate utf8mb4_spanish2_ci;
-- Activar la Base de Datos
USE ejercicio5;

-- Crear la tabla personas
CREATE TABLE personas (
    idPersona INT AUTO_INCREMENT,
    dniPersona VARCHAR(9),
    nombrePersona VARCHAR(45),
    primerApellidoPersona VARCHAR(45),
    segundoApellidoPersona VARCHAR(45),
    telefonoPersona VARCHAR(9),
    correoElectronicoPersona VARCHAR(100),
    direccionPersonaPersona VARCHAR(100),
    PRIMARY KEY (idPersona)
);

-- Crear la tabla animales
CREATE TABLE animales (
    idAnimal INT AUTO_INCREMENT,
    microchipAnimal DECIMAL(20 , 0 ),
    nombreAnimal VARCHAR(45),
    razaAnimal VARCHAR(45),
    idPersonaFK INT,
    PRIMARY KEY (idAnimal),
    FOREIGN KEY (idPersonaFK)
        REFERENCES personas (idPersona)
);

-- Modificar tabla personas
-- Añadir al final el campo localidad
ALTER TABLE personas ADD localidadPersona VARCHAR (45);
-- Añadir detrás de teléfono el campo móvil
ALTER TABLE personas ADD movilPersona VARCHAR (9) AFTER telefonoPersona;
-- Cambiar el nombre al teléfono por fijo
ALTER TABLE personas CHANGE telefonoPersona fijoPersona VARCHAR(9);
-- Borrar el campo dirección
ALTER TABLE personas DROP direccionPersonaPersona;
-- Insertar registros en personas
-- PRIMER MÉTODO DE INSERT DATOS:
INSERT INTO personas VALUES (
	null, '12345678Z', 'Fulanito', 'Sánchez', 'Solís', '954954954', '654654654', 'fulanito@gmail.com', 'Sevilla'
);
-- SEGUNDO MÉTODO DE INSERT DATOS: (Modificando manualmente, luego apply and close)
SELECT * from personas;


